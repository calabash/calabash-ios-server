language: objective-c

before_script:
  - gem install xcpretty

script:
  - make clean
  - make framework
  - make frank
  # Dylib creation is allowed by Xcode 6, but it requires
  # code-signing which is not easy to set up on Travis CI.
  #- make dylibs
  #- make all
  # The CoreSimulator environment on Travis CI is not ready
  # for calabash testing.  The 'chou' tests are cucumber
  # tests.
  #- scripts/ci/travis/run-chou-tests.rb

# There are some problems with Xcode 6 + rvm on Travis CI.
# In particular 2.1.1 does not work.
# Use the default ruby (2.0.0) until the rvm env stabilizes.
#rvm:
#  - 2.0.0

notifications:
  email:
    recipients:
      - joshuajmoody@gmail.com
      - karl.krukow@xamarin.com
    on_success: change
    on_failure: always
